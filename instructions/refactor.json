{
  "Analyse": "⚠️ BUDGET: MAX 8 tool-kald i Analyse. Din plan må kun indeholde: (1) list_symbols, (2-5) read_location MAX 4, (6) write_file(refactor_analyse.md), (7) done. Brug IKKE list_files, create_todo, analyze_dependencies, eller update_todo. Notér SOLID-analyse og modul-kortlægning direkte i write_file-indholdet — ikke som separate trin.\n\nTRIN 0 (FØR du læser): Gruppér symbolerne fra list_symbols EFTER NAVNEMØNSTER: `is_*`, `get_*`, `set_*`, `read_*`, `write_*`, `*file*`, `*config*`, `*user*`, `*handler*`, `*process*`, `*route*` — match grupper til moduler. Dette fanger ALLE symboler uden at læse dem.\n\nTRIN 1: Kald list_symbols(filepath='{source_file}') og notér ALLE symboler med deres navn og type.\nTRIN 2-5: Læs KUN de 3-4 tvivlsomme funktioner/klasser med read_location() — dem du ikke kunne gruppere 100% sikkert ud fra navnet alene. Spring over de symboler der har et tydeligt navnemønster.\nTRIN 6: Skriv analysen med write_file(path='refactor_analyse.md', content='...'). Gem i RODEN (ikke docs/).\n\n⛔ FORMAT — refactor_analyse.md SKAL bruge dette EKSAKTE format:\n\n```\n## Oversigt\n[Filens overordnede struktur og anbefalede moduler. 2-5 sætninger.]\n\n## Symboler per modul\n\n### Module: config.py\n**Ansvar:** Konfigurationshåndtering, miljøvariabler\n\n| Navn | Type | Linje |\n|------|------|-------|\n| DATABASE_URL | variable | 12 |\n| MAX_RETRIES | variable | 13 |\n| get_config | function | 25 |\n\n### Module: user_handler.py\n**Ansvar:** Brugeradministration\n\n| Navn | Type | Linje |\n|------|------|-------|\n| User | class | 45 |\n| create_user | function | 85 |\n```\n\nVIKTIGE REGLER:\n- ALTID `### Module: filename.py` som modul-overskrift (IKKE 'Modul', IKKE '##')\n- ALTID `**Ansvar:**` linje efter overskriften\n- ALTID tabel med præcis 3 kolonner: Navn, Type, Linje\n- ALLE symboler SKAL være i tabellen — ingen symboler må mangle\n- INGEN ekstra formatering mellem modulerne (ingen kodeblokke, ingen lister)\n- Symbolernes linjenumre skal matche l...",
  "en_Analyse": "⚠️ BUDGET: MAX 8 tool calls in Analysis. Your plan may only include: (1) list_symbols, (2-5) read_location MAX 4, (6) write_file(refactor_analyse.md), (7) done. Do NOT use list_files, create_todo, analyze_dependencies, or update_todo. Note SOLID analysis and module mapping directly in the write_file content — not as separate steps.\n\nSTEP 0 (BEFORE reading): Group symbols from list_symbols BY NAME PATTERN: `is_*`, `get_*`, `set_*`, `read_*`, `write_*`, `*file*`, `*config*`, `*user*`, `*handler*`, `*process*`, `*route*` — match groups to modules. This catches ALL symbols without reading them.\n\nSTEP 1: Call list_symbols(filepath='{source_file}') and note ALL symbols with their name and type.\nSTEP 2-5: Read ONLY the 3-4 uncertain functions/classes with read_location() — those you couldn't group 100% safely by name alone. Skip symbols with clear name patterns.\nSTEP 6: Write analysis with write_file(path='refactor_analyse.md', content='...'). Save in ROOT (not docs/).\n\n⛔ FORMAT — refactor_analyse.md MUST use this EXACT format:\n\n```\n## Overview\n[File structure and recommended modules. 2-5 sentences.]\n\n## Symbols per module\n\n### Module: config.py\n**Responsibility:** Configuration handling, environment variables\n\n| Name | Type | Line |\n|------|------|------|\n| DATABASE_URL | variable | 12 |\n| MAX_RETRIES | variable | 13 |\n| get_config | function | 25 |\n\n### Module: user_handler.py\n**Responsibility:** User management\n\n| Name | Type | Line |\n|------|------|------|\n| User | class | 45 |\n| create_user | function | 85 |\n```\n\nIMPORTANT RULES:\n- ALWAYS use `### Module: filename.py` as module heading (NOT 'Modul', NOT '##')\n- ALWAYS include `**Responsibility:**` line after heading\n- ALWAYS table with exactly 3 columns: Name, Type, Line\n- ALL symbols MUST be in the table — no missing symbols\n- NO extra formatting between modules (no code blocks, no lists)",
  "es_Analyse": "⚠️ PRESUPUESTO: MAX 8 llamadas a herramientas en Análisis. Tu plan solo puede incluir: (1) list_symbols, (2-5) read_location MAX 4, (6) write_file(refactor_analyse.md), (7) done. NO uses list_files, create_todo, analyze_dependencies, ni update_todo. Anota el análisis SOLID y mapeo de módulos directamente en el contenido de write_file — no como pasos separados.\n\nPASO 0 (ANTES de leer): Agrupa los símbolos de list_symbols POR PATRÓN DE NOMBRE: `is_*`, `get_*`, `set_*`, `read_*`, `write_*`, `*file*`, `*config*`, `*user*`, `*handler*`, `*process*`, `*route*` — empareja grupos con módulos. Esto captura TODOS los símbolos sin leerlos.\nPASO 1: Llama list_symbols(filepath='{source_file}') y anota TODOS los símbolos con nombre y tipo.\nPASO 2-5: Lee SOLO las 3-4 funciones/clases dudosas con read_location() — aquellas que no pudiste agrupar 100% seguro solo por nombre.\nPASO 6: Escribe análisis con write_file(path='refactor_analyse.md', content='...'). Guarda en RAÍZ (no docs/).",
  "zh_Analyse": "⚠️ 预算：分析阶段最多 8 次工具调用。你的计划只能包括：(1) list_symbols，(2-5) read_location 最多 4 次，(6) write_file(refactor_analyse.md)，(7) done。不要使用 list_files、create_todo、analyze_dependencies 或 update_todo。将 SOLID 分析和模块映射直接记入 write_file 内容中 — 不作为单独的步骤。\n\n步骤 0（在阅读之前）：按名称模式对 list_symbols 的符号进行分组：`is_*`、`get_*`、`set_*`、`read_*`、`write_*`、`*file*`、`*config*`、`*user*`、`*handler*`、`*process*`、`*route*` — 将组与模块匹配。这无需阅读即可捕获所有符号。\n步骤 1：调用 list_symbols(filepath='{source_file}') 并记录所有符号的名称和类型。\n步骤 2-5：仅使用 read_location() 读取 3-4 个不确定的函数/类 — 那些仅凭名称无法 100% 确定分组的符号。\n步骤 6：使用 write_file(path='refactor_analyse.md', content='...') 编写分析。保存在根目录（而非 docs/）。",
  "Plan": "Læs `refactor_analyse.md` med read_chunk() for at få den fulde analyse — du behøver IKKE læse symboler/funktioner igen. Beslut modulopdeling og skriv planen med write_file(path='refactor_plan.md', content='...').\n\n⛔ FORMATKRAV — planen SKAL bruge dette EKSAKTE format (ingen variationer):\n\n```\n## Oversigt\nKort overblik over modulopdelingen.\n\n## Module: config.py\n**Ansvar:** Konfigurationshåndtering, miljøvariabler\n**Symboler (10):** DATABASE_URL, MAX_RETRIES, DEFAULT_TIMEOUT, ALLOWED_EXTENSIONS, LOG_LEVEL, get_config, is_extension_allowed, validate_config, get_allowed_extensions, config_to_dict\n\n## Module: user_handler.py\n**Ansvar:** Brugeradministration\n**Symboler (10):** User, create_user, find_user, find_users_by_name, format_users, get_user_names, get_active_users, bulk_create_users, user_count_by_status, find_user_by_email\n```",
  "en_Plan": "Read `refactor_analyse.md` with read_chunk() to get the full analysis — you do NOT need to read symbols/functions again. Decide module division and write the plan with write_file(path='refactor_plan.md', content='...').\n\n⛔ FORMAT REQUIREMENTS — the plan MUST use this EXACT format (no variations):\n\n```\n## Overview\nBrief overview of the module division.\n\n## Module: config.py\n**Responsibility:** Configuration handling, environment variables\n**Symbols (10):** DATABASE_URL, MAX_RETRIES, DEFAULT_TIMEOUT, ALLOWED_EXTENSIONS, LOG_LEVEL, get_config, is_extension_allowed, validate_config, get_allowed_extensions, config_to_dict\n\n## Module: user_handler.py\n**Responsibility:** User management\n**Symbols (10):** User, create_user, find_user, find_users_by_name, format_users, get_user_names, get_active_users, bulk_create_users, user_count_by_status, find_user_by_email\n\n## Module: processor.py\n**Responsibility:** Data processing\n**Symbols (10):** parse_csv, filter_records, sort_records, merge_data, count_by_key, batch_process, group_by_key, validate_records, rename_key, deduplicate_records\n```\n\nIMPORTANT RULES:\n- ALWAYS `## Module: filename.py` (NOT other variants)\n- ALWAYS `**Responsibility:**` on next line with one-sentence description\n- ALWAYS `**Symbols (N):**` on the following line with ALL symbols comma-separated\n- Symbols MUST be on ONE line — no bullets, no code blocks\n- Each module MUST have exactly 3 lines: heading, responsibility, symbols\n- NO other formatting between module sections (no tables, no lists)\n\n📋 Start by calling **plan_phase**. The system auto-completes this phase as soon as refactor_plan.md is written. <<<DONE>>> only after the plan is written.",
  "es_Plan": "Lee `refactor_analyse.md` con read_chunk() para obtener el análisis completo — NO necesitas leer símbolos/funciones de nuevo. Decide la división de módulos y escribe el plan con write_file(path='refactor_plan.md', content='...').\n\nFORMATO: Usa `## Module: filename.py` con `**Responsibility:**` y `**Symbols (N):` en líneas siguientes. Todos los símbolos en UNA línea separados por comas. Sin listas ni bloques de código.\n\n📋 Comienza llamando a **plan_phase**. El sistema auto-completa esta fase tan pronto como se escriba refactor_plan.md. <<<DONE>>> solo después de escribir el plan.",
  "zh_Plan": "使用 read_chunk() 读取 `refactor_analyse.md` 以获取完整分析 — 你不需要重新读取符号/函数。决定模块划分并使用 write_file(path='refactor_plan.md', content='...') 编写计划。\n\n格式：使用 `## Module: filename.py`，后跟 `**Responsibility:**` 和 `**Symbols (N):` 行。所有符号用逗号分隔在一行上。没有列表或代码块。\n\n📋 从调用 **plan_phase** 开始。系统会在 refactor_plan.md 写入后自动完成此阶段。仅在计划编写后使用 <<<DONE>>>。",
  "Ekstraher": "Opret NYE .py modulfiler med batch_extract_symbols() i.h.t. planen fra forrige fase. Følg refactor_plan.md TIL PUNKT OG PRIKKE — ALLE moduler nævnt i planen SKAL oprettes. 🔥 DETERMINISTISKE VÆRKTØJER: Brug batch_extract_symbols(source='{source_file}', symbols='_RateLimiter, _is_development_mode, _rate_limit', target='middleware.py') for at flytte FLERE symboler i ét kald — systemet kopierer alle symboler inkl. imports, fjerner dem fra {source_file}, OG tilføjer imports i {source_file}. Brug extract_symbol kun hvis batch_extract_symbols fejler, eller til enkelt-symboler. Brug verify_refactor(source='{source_file}') bagefter for at bekræfte syntaks. Du skal oprette FILER som routes.py, session_manager.py, file_handler.py osv. — IKKE write_file til refactor_plan.md (den findes allerede fra Plan-fasen og er auto-indlæst i din kontekst ovenfor).\n📋 Din opgaveplan (via **plan_phase**) skal indeholde konkrete tool-kald med parametre, f.eks.:\n  `batch_extract_symbols(source='refac_test.py', symbols=['DATABASE_URL', 'MAX_RETRIES', ...], target='config.py')`\n  `verify_refactor(source='refac_test.py')`\nAngiv symbolnavnene eksplicit i hvert trin så rækkefølgen og indholdet er tydeligt.\n🔥 EFFEKTIVITETSGUIDE: Udfør trinene i din plan i rækkefølge. Du har KUN 20 iterations. Systemet auto-afslutter denne fase så snart ALLE moduler nævnt i planen er oprettet. 📋 Brug **update_todo** for at markere hvert modul færdigt.",
  "en_Ekstraher": "Create NEW .py module files with batch_extract_symbols() following the plan from the previous phase. Follow refactor_plan.md TO THE LETTER — ALL modules mentioned in the plan MUST be created. 🔥 DETERMINISTIC TOOLS: Use batch_extract_symbols(source='{source_file}', symbols='_RateLimiter, _is_development_mode, _rate_limit', target='middleware.py') to move MULTIPLE symbols in one call — the system copies all symbols including imports, removes them from {source_file}, AND adds imports in {source_file}. Use extract_symbol only if batch_extract_symbols fails, or for single symbols. Use verify_refactor(source='{source_file}') afterward to confirm syntax. Create FILES like routes.py, session_manager.py, file_handler.py etc. — do NOT write_file to refactor_plan.md (it already exists from the Plan phase and is auto-loaded in your context above).\n📋 Your task plan (via **plan_phase**) should include concrete tool calls with parameters, e.g.:\n  `batch_extract_symbols(source='{source_file}', symbols=['DATABASE_URL', 'MAX_RETRIES', ...], target='config.py')`\n  `verify_refactor(source='{source_file}')`\nSpecify symbol names explicitly in each step so the order and content is clear.\n🔥 EFFICIENCY GUIDE: Execute the steps in your plan in order. You have ONLY 20 iterations. The system auto-completes this phase as soon as ALL modules mentioned in the plan are created. 📋 Use **update_todo** to mark each module done.",
  "es_Ekstraher": "Crea NUEVOS archivos de módulo .py con batch_extract_symbols() siguiendo el plan de la fase anterior. Sigue refactor_plan.md AL PIE DE LA LETRA — TODOS los módulos mencionados en el plan DEBEN crearse. Usa herramientas deterministas. Crea archivos como routes.py, session_manager.py, file_handler.py.\nTienes SOLO 20 iteraciones. El sistema auto-completa esta fase cuando TODOS los módulos del plan estén creados.",
  "zh_Ekstraher": "使用 batch_extract_symbols() 创建新的 .py 模块文件，遵循上一阶段的计划。严格按照 refactor_plan.md 执行 — 计划中提到的所有模块都必须创建。使用确定性工具。创建 routes.py、session_manager.py、file_handler.py 等文件。你只有 20 次迭代。当计划中的所有模块都创建完成后，系统会自动完成此阶段。",
  "Opdatér": "Opdater {source_file} med remove_symbol() og add_import() — DETERMINISTISKE værktøjer der ikke kræver LLM. Brug remove_symbol(source='{source_file}', symbol_name='FunktionsNavn') for at fjerne et symbol der allerede er flyttet til et modul. Brug add_import(source='{source_file}', module='routes', symbol='FunktionsNavn') for at tilføje en import til et modul. Brug verify_refactor(source='{source_file}') til at tjekke syntaks. 🔥 EFFEKTIVITETSGUIDE: Din plan viser rækkefølgen: remove_symbol → add_import → verify_refactor for hvert modul. Du har KUN 20 iterations. Brug edit_file KUN hvis remove_symbol/add_import ikke kan klare opgaven (f.eks. ved komplekse sektioner der ikke er rene symboler). 📋 Brug **update_todo** for at markere fremdrift.\n<<<DONE>>> først når {source_file} er syntaktisk gyldig og alle relevante imports er tilføjet.",
  "en_Opdatér": "Update {source_file} with remove_symbol() and add_import() — DETERMINISTIC tools that don't require the LLM. Use remove_symbol(source='{source_file}', symbol_name='FunctionName') to remove a symbol already moved to a module. Use add_import(source='{source_file}', module='routes', symbol='FunctionName') to add an import to a module. Use verify_refactor(source='{source_file}') to check syntax. 🔥 EFFICIENCY GUIDE: Your plan shows the order: remove_symbol → add_import → verify_refactor for each module. You have ONLY 20 iterations. Use edit_file ONLY if remove_symbol/add_import cannot handle the task (e.g., complex sections that aren't clean symbols). 📋 Use **update_todo** to mark progress.\n<<<DONE>>> only after {source_file} is syntactically valid and all relevant imports are added.",
  "es_Opdatér": "Actualiza {source_file} con remove_symbol() y add_import() — herramientas deterministas. use remove_symbol(source='{source_file}', symbol_name='NombreFunción') y add_import(source='{source_file}', module='routes', symbol='NombreFunción'). Solo 20 iteraciones. usa edit_file SOLO si remove_symbol/add_import no pueden manejar la tarea.\n<<<DONE>>> solo después de que {source_file} sea sintácticamente válido.",
  "zh_Opdatér": "使用 remove_symbol() 和 add_import() 更新 {source_file} — 确定性工具。仅当 remove_symbol/add_import 无法处理任务时才使用 edit_file。只有 20 次迭代。仅在 {source_file} 语法有效且所有相关导入已添加后才使用 <<<DONE>>>。",
  "Test": "Kør testsuiten med run_tests(). Hvis tests fejler, ret import-stier med edit_file() og genkør. Bliv ved indtil ALLE tests består. Opdater issue-status til 'resolved' med update_issue_status() når tests består. 📋 Brug **update_todo** for at markere fremdrift.",
  "en_Test": "Run the test suite with run_tests(). If tests fail, fix import paths with edit_file() and rerun. Keep going until ALL tests pass. Update issue status to 'resolved' with update_issue_status() when tests pass. 📋 Use **update_todo** to mark progress.",
  "es_Test": "Ejecuta la suite de pruebas con run_tests(). Si fallan, corrige rutas de importación con edit_file() y vuelve a ejecutar. Continúa hasta que TODAS las pruebas pasen. Actualiza el estado del issue a 'resolved' con update_issue_status().",
  "zh_Test": "使用 run_tests() 运行测试套件。如果测试失败，使用 edit_file() 修复导入路径并重新运行。持续直到所有测试通过。当测试通过时，使用 update_issue_status() 将问题状态更新为 'resolved'。"
}
